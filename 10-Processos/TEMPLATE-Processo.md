---
tags: [processos]
status: rascunho
data: AAAA-MM-DD
modulo:
---

# AAAA-MM-DD - nome curto do processo

> Copie este arquivo, renomeie, preencha. `modulo` no frontmatter: `cliente`,
> `servidor`, `shared`, `docs` (pode ser mais de um). `status`: `rascunho`,
> `feito`, `parcial`.

## Como era

O que o jogador via (ou o que o codigo fazia) **antes**. Seja concreto:
qual tecla, qual tela, o que incomodava.

## O que o jogador ve agora

O "depois" em linguagem de jogo, sem jargao de C++.

## O que implementei (e por que)

Para cada mudanca relevante: o que mudou no codigo e **por que** daquele
jeito (nao so o "o que"). Se um experimento foi revertido, anote — isso
tambem e aprendizado.

## Arquivos tocados

No repo do jogo (`C:\Source Priston\Source Priston`), nao neste vault:

- `SrcGame/...`
- `SrcServer/...` (se houver)
- `Shared/...` (se houver)
- assets no cliente: `C:\Cliente Full\...` (se houver)

## Shared / protocolo

- Tocou em `Shared/`? [sim/nao]
- `smTRANSCODE_*` novos ou alterados? (se nao, escreva "nenhum")

## O que testar

1.
2.

## O que ficou de fora / proximos passos

O que voce viu e **nao** fez de proposito (escopo). O que ainda quer
ajustar depois.

## Aprendizado

Conceito de C++/ImGui/rede que essa tarefa ensinou, em uma ou duas frases.
O que ainda confunde.
