# Configuração local do Obsidian (não deve ser compartilhada)
# - o .obsidian da RAIZ: ignora só o workspace (config local de janelas)
.obsidian/workspace*.json
.obsidian/.trash/
# - .obsidian em subpastas: são resíduos de vaults abertos dentro do vault, ignora tudo
**/.obsidian/

# Arquivos do jogo (cliente full, source zipada, .exe/.pdb) — MB/GB demais
# pra este repo de documentação. Ficam só na máquina.
Arquivos do Jogo/

# Temporários do sistema
.DS_Store
Thumbs.db
*.swp
~$*
